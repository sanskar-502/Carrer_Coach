const { PrismaClient } = require('@prisma/client'); const db = new PrismaClient(); db.industryInsight.create({ data: { industry: 'test', salaryRanges: [], growthRate: 5, demandLevel: 'Medium', topSkills: [], marketOutlook: 'Neutral', keyTrends: [], recommendedSkills: [], lastUpdated: new Date(), nextUpdate: new Date() } }).then(console.log).catch(console.error).finally(() => db.$disconnect());
